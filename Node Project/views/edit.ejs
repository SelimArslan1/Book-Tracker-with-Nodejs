<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book Details</title>
    <link rel="stylesheet" href="/css/editBookStyles.css">
</head>
<body>
    <script src="/javascript/script.js"></script>
    <div class="container">
        <a href="/books" class="back-btn">Back</a>
        <h1>Edit Book Details</h1>
        <form action="/books/edit/<%= book._id%>" method="POST" class="form-container">
            <label for="title">Book Title:</label>
            <input type="text" id="title" name="title" value="<%= book.title %>" required>

            <label for="author">Author:</label>
            <input type="text" id="author" name="author" value="<%= book.author %>" required>

            <label for="genre">Genre:</label>
            <input type="text" id="genre" name="genre" value="<%= book.genre %>">

            <label for="totalPages">Total Pages:</label>
            <input type="number" id="totalPages" name="totalPages" value="<%= book.totalPages %>" required>

            <label for="currentPage">Current Page:</label>
            <input type="number" id="currentPage" name="currentPage" value="<%= book.currentPage %>">

            <label for="status">Status:</label>
            <select id="status" name="status">
                <option value="to-read" <% if (book.status === "to-read") { %>selected<% } %>>To Read</option>
                <option value="reading" <% if (book.status === "reading") { %>selected<% } %>>Reading</option>
                <option value="finished" <% if (book.status === "finished") { %>selected<% } %>>Finished</option>
            </select>

            <label for="format">Format:</label>
            <select id="format" name="format">
                <option value="ebook" <% if (book.format === "ebook") { %>selected<% } %>>Ebook</option>
                <option value="paperback" <% if (book.format === "paperback") { %>selected<% } %>>Paperback</option>
                <option value="audiobook" <% if (book.format === "audiobook") { %>selected<% } %>>Audiobook</option>
            </select>

            <button type="submit" class="submit-btn">Update Book</button>
        </form>
    </div>
</body>
</html>
